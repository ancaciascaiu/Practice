# A palindrome is a word, phrase, number, or other sequence of
# characters which reads the same backward or forward, such as
# madam or kayak.
#This time, implementing it using an array of numbers as input.

def palindrome?(array)
    #find the midpoint of the array. This will be different for odd or even array lengths.
        #if array.length is odd,
end

p palindrome?([1,7,8,7,1]) # => true
# p palindrome?([1,2,8,8,2,1]) # => true
# p palindrome?([1,2,8,7,1]) # => false
